# Dedicated environment for backend integration tests.
# The compose-based test runner does not read this file, but it remains a handy
# reference if you need to point a manual Drizzle push or local script at the
# dedicated test database. Do not reuse your development database.

DATABASE_URL="postgresql://postgres:password@localhost:5433/matplant_test"
MEILISEARCH_HOST="http://127.0.0.1:7700"
MEILISEARCH_KEY="test-key"
